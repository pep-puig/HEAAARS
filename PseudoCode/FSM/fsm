from abc import ABC, abstractmethod
from enum import Enum, auto

# All core objects here:
class StateEnum(Enum):
    INIT = auto()
    TAKEOFF = auto()
    FLY_TO_TARGET = auto()
    LANDING_TARGET = auto()
    SWIM_TO_TARGET = auto()
    RETURN_HOME = auto()
    LANDING_HOME = auto()

class RobotSystem:
    # contains vehicles + all handlers + commanders
    ...

class State(ABC):
    # abstract state's run() and next_state
    ...

class FSM:
    # manages transitions using StateEnum keys
    ...